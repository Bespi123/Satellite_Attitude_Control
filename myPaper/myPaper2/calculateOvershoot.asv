function [overshoot,to] = calculateOvershoot(y,sp,initialConditions)
    overshoot = zeros(1,length(sp));
    to = zeros(1,length(sp));
    for i = 1:length(sp)
        if sp(i)>
            signalRange = max(y(:,i))-min(y(:,i));
            optimalRange = 
        elseif sp(i)<0
            [overshoot(i),to(i)] = min(y(:,i));
        else
            [overshoot(i),to(i)] = max(abs(y(:,i)));
        end
    end 
end